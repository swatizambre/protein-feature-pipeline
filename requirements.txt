# Protein Feature Pipeline — Python dependencies
# Prefer:  pip install -e ".[all]"
# Minimal: pip install -e .
# Or from this file: pip install -r requirements.txt

# --- core (required) ---
numpy>=1.23

# --- recommended (viz + analysis + bio + web + tests) ---
matplotlib>=3.5
pandas>=2.0
scipy>=1.10
scikit-learn>=1.2
seaborn>=0.12
tabulate>=0.9
biopython>=1.81
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pytest>=7.0
ruff>=0.1
black>=23.0
build>=1.0
httpx>=0.27

# --- optional (not in .[all]; install if needed) ---
# rdkit>=2022.9          # ligand SMILES in protein-features-pocket
